<div class="container">
	<h5>Review for <%= @school.name %></h5>
			<%= form_for [@school, @review] do |f| %>
				<%= f.text_area :text %><br />
				<%= f.submit 'submit review' %> <br />
			<% end %>
			<br />
			<%= link_to "home", schools_path(school_id: @school.id) %>
</div>

 